# 강의 생성 통합 실행 점검

`sample2.pptx` 3장으로 PPT 분석부터 강의 영상, 핵심 요약, O/X 퀴즈 생성까지 연결해 확인했습니다.

## 결과

| 확인 항목 | 결과 |
| --- | --- |
| 슬라이드 변환 | 3장 모두 PDF·PNG 변환 성공 |
| 검색·대본·음성 | 슬라이드별 1회씩 검색, 설명문·대본 및 음성 생성 완료 |
| 최종 영상 | 1920×1080, H.264/AAC, 228.67초, 5,588,251바이트 |
| 영상 파일 검사 | 전체 디코딩 오류 없이 완료 |
| 학습 보조 자료 | 핵심 요약 및 정답·해설을 포함한 O/X 퀴즈 3개 |
| 키 없는 테스트 | 38개 통과 |

슬라이드별 음성과 영상의 길이는 각각 **77.160초, 67.656초, 83.808초**로 일치했습니다. 최종 영상과 개별 영상 길이 합의 차이는 약 0.043초입니다.

## 실행 조건

- 점검일: 2026-09-09
- 입력: 저장소의 `sample2.pptx` - 모델 성능 모니터링, Concept Drift, 모델 모니터링 아키텍처
- 실행: `lecture_agent.ipynb`의 함수와 실제 LangGraph 흐름을 불러와 로컬 도구 경로를 연결한 통합 시험
- 외부 연동: Tavily Search, OpenAI GPT-4o-mini, GPT-4o-mini-tts(Alloy)
- API 호출: 검색 3회 / 텍스트 8회 / 음성 3회, 자동 재시도 없음
- 도구: Windows, LibreOffice 26.8.0.3, Poppler, FFmpeg/FFprobe 9.0.1
- 관측 실행 시간: 119.24초. 이 환경에서의 단일 실행 기록이며 제작 시간 절감률은 아닙니다.
- 입력 SHA256: `b01ec74c77317432196c696b368fe8a389f5b995f915f5d2a2618c3e429e6554`
- 실행 노트북 SHA256: `ab9bbe196df3cd6fe324bb0b68340e58b938f08fcac5c944e0e948ccadb47f13`

## 문서 변환 문제와 해결

Windows의 긴 작업 폴더 안에 LibreOffice 프로필을 만들었을 때 변환 프로세스가 종료되는 현상을 확인했습니다. 입력·출력 경로를 유지하고 프로필만 짧은 임시 폴더로 분리하자 3장 전체가 변환됐습니다. 이 방식을 실행 코드에 반영하고 회귀 테스트를 추가했습니다.

## 확인 범위와 후속 개선

PPT 변환 이미지와 최종 영상의 슬라이드 화면, 생성된 대본·요약·퀴즈를 점검했습니다. 음성·영상 길이 및 파일 디코딩 검사는 전체 파일에 수행했습니다. Gradio 브라우저 화면의 업로드·다운로드 조작과 전체 음성의 청취 평가는 이번 통합 시험에 포함하지 않았습니다.

대본에는 반복 인사와 실제 자료에 없는 '다음 시간' 예고가 남아 있습니다. 배치·실시간 모니터링 비용을 비교하는 퀴즈도 자료의 비교 조건이 더 잘 드러나도록 표현을 다듬을 수 있습니다. 후속 개선은 대본 검토 단계와 문맥별 표현 점검에 집중합니다. 한 PPT의 실행 성공을 모든 입력의 내용 정확성이나 학습 효과로 확대 해석하지 않습니다.

API 키와 로컬 경로를 포함한 실행 환경 파일은 공개 대상에서 제외합니다. 기존 `결과영상.mp4`는 보존하며, 이번 점검 영상으로 교체하지 않았습니다.
